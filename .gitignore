# Dependencies
/node_modules

# Next.js build output
/.next
# Isolated production build used to verify a build next to a running dev server
# (see README → Troubleshooting): NEXT_DIST_DIR=.next-build npm run build
/.next-build
/out
/build

# Production
/dist

# Local environment files — NEVER commit real keys
.env
.env.local
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS / editor noise
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
